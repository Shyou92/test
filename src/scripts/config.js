const formulaFirstSpan = document.querySelector('#formulaFirstSpan');
const aFormulaElement = document.querySelector('#aElement');
const xFormulaElement = document.querySelector('#xElement');
const yFormulaElement = document.querySelector('#yElement');
const bFormulaElement = document.querySelector('#bElement');
const formulaDots = document.querySelector('#cardFormulaDots');
const formulaSecondSpan = document.querySelector('#formulaSecondSpan');
const parentNodeAElement = document.querySelector('.parentElem');
const cardFormulaAnswer = document.querySelector('.card__formula-answer');
const card = document.querySelector('.card');
const firstGroup = document.querySelectorAll('.card__formula-first');
const secondGroup = document.querySelectorAll('.card__formula-second');
const btnForward = document.querySelector('.card__button-container-forward');
const templateMarks = document.querySelector('#template-marks').content;

export {
  aFormulaElement,
  formulaFirstSpan,
  xFormulaElement,
  bFormulaElement,
  yFormulaElement,
  parentNodeAElement,
  cardFormulaAnswer,
  card,
  firstGroup,
  secondGroup,
  formulaDots,
  formulaSecondSpan,
  btnForward,
  templateMarks,
};
